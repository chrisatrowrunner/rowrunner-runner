{
  "name": "rowrunner-runner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RowRunner — runner dispatch app. The venue-staff view that claims and delivers fan orders. Pairs with the fan ordering PWA over a shared Supabase backend.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
